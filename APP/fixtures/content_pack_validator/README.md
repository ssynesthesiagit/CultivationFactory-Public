# CPK-1 Validator Fixtures

All fixtures are isolated developer-test data. Candidate status: `CANDIDATE_NON_AUTHORITATIVE`. Nested invalid ZIPs intentionally contain unsafe member names so the validator can prove fail-closed archive handling; they must never be extracted.
