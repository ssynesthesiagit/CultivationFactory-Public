# The accepted APP tree is immutable binary authority, including its original text encodings.
APP/** -text

# CI orchestration is authored with stable LF endings on every runner.
.gitattributes text eol=lf
.github/workflows/*.yml text eol=lf
ci/** text eol=lf
